import { css } from "@emotion/css";

export const headerContainerStyles = css({
  display: "flex",
  justifyContent: "space-between",
});

export const headerHomeLogoButtonStyles = css({
  padding: 0,
  margin: 0,
  background: "none",
  border: "none",
  cursor: "pointer",
});

export const headerFavoritesButtonStyles = css({
  padding: "5px 15px",
  height: "max-content",
  borderRadius: 20,
  margin: 0,
  background: "none",
  border: "none",
  cursor: "pointer",
  backgroundColor: "#232524",
  color: "#FF6871",
  textDecoration: "none",
});
